package entity_class;

public class Types {
    public enum UserRole { STUDENT, COMPANY_REPRESENTATIVE, CAREER_CENTER_STAFF }
    public enum Major { CCDS, COE, NBS, SPMS, SBS, WKWSCI, COHASS }
    public enum ApplicationStatus { PENDING, SUCCESSFUL, UNSUCCESSFUL }
    public enum InternshipStatus { PENDING, APPROVED, REJECTED, FILLED }
    public enum InternshipLevel { BASIC, INTERMEDIATE, ADVANCED }
    public enum WithdrawalStatus { PENDING, APPROVED, REJECTED }
}
